/* * @type {import('next').NextConfig} */
// Next.js configuration - keeping it minimal and clean
const nextConfig = {
  reactStrictMode: true, // Helps catch potential problems during development
};
module.exports = nextConfig;
